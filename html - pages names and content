html pages:

  1) welcome (html page)
  2) register_user [access via welcome] (html page)
  3) register_librarian [access via welcome] (html page)
  4) user_homepage: (html page)
    a) homepage:(homepage content)
        * user info (user homepage content)
        * menu bar: (user homepage content)
          - new_borrow + order_book (user homepage menu content)
          - my_books + borrow_extension + borrows_history (user homepage menu content)
          - order_status (user homepage menu content)
     b) new_borrow + order_book (html page)
     c) my_books + borrow_extension + borrows_history (html page)
     d) order_status (html page)
   
   5) librarian_homepage: (html page)
    a) homepage:
        * librarian info (librarian homepage content)
        * menu bar: (librarian homepage content)
          - accept_borrow + accept_extension (librarian homepage menu content)
          - add_NewBook (librarian homepage menu content)
     b) accept_borrow + accept_extension (html page)
     c) add_NewBook (html page)
      
